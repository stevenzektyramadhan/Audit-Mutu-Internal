<?php defined('BASEPATH') OR exit('No direct script access allowed'); include APPPATH . 'views/layouts/header.php'; include APPPATH . 'views/layouts/sidebar.php'; $follow_up = isset($follow_up) ? $follow_up : NULL; ?>
<div class="ami-panel"><div class="ami-panel-body"><?php echo validation_errors('<div class="alert alert-danger">', '</div>'); ?><h2 class="ami-section-title mb-4"><?php echo html_escape($title); ?></h2><?php echo form_open($action); ?><?php if (!$follow_up): ?><div class="form-group"><label for="decision_id">Keputusan RTM resolved</label><select id="decision_id" class="form-control" disabled><option><?php foreach ($decisions as $decision): ?><?php if ((int) $decision->id === (int) $decision_id): ?><?php echo html_escape($decision->meeting_code . ' / #' . $decision->display_order . ' — ' . $decision->decision_text); ?><?php endif; ?><?php endforeach; ?></option></select></div><?php endif; ?><div class="form-group"><label for="responsible_user_id">Penanggung jawab</label><select id="responsible_user_id" name="responsible_user_id" class="form-control" required><?php foreach ($users as $user): ?><option value="<?php echo (int) $user->id; ?>"<?php echo $follow_up && (int) $follow_up->responsible_user_id === (int) $user->id ? ' selected' : ''; ?>><?php echo html_escape($user->nama . ' — ' . $user->role); ?></option><?php endforeach; ?></select></div><div class="form-group"><label for="due_date">Batas waktu</label><input id="due_date" name="due_date" type="date" class="form-control" value="<?php echo html_escape($follow_up ? $follow_up->due_date : ''); ?>"></div><div class="form-group"><label for="follow_up_note">Catatan tindak lanjut</label><textarea id="follow_up_note" name="follow_up_note" class="form-control" maxlength="10000"><?php echo html_escape($follow_up ? $follow_up->follow_up_note : ''); ?></textarea></div><button class="btn-ami" type="submit">Simpan</button> <a class="btn-ami btn-outline-ami" href="<?php echo site_url('lpmpi/spmi-follow-ups'); ?>">Batal</a></form></div></div>
<?php include APPPATH . 'views/layouts/footer.php'; ?>
